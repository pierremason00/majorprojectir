local_costmap:
  global_frame: odom
  robot_base_frame: base_link

  update_frequency: 5.0
  publish_frequency: 5.0

  static_map: false          # uses live /scan instead
  rolling_window: true

  width: 3.0                 # 3 m × 3 m window around robot
  height: 3.0
  resolution: 0.05           # 5 cm cells

  transform_tolerance: 0.5
