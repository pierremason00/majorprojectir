global_costmap:
  global_frame: map
  robot_base_frame: base_link

  update_frequency: 1.0
  publish_frequency: 1.0

  static_map: true
  rolling_window: false

  # Size comes from the static map, so no width/height here
  transform_tolerance: 0.5
